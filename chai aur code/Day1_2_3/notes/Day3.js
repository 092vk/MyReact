// 1. In react all the components are finally inserted into the index.html file , thus it is called a single page application

// 2. when you make components , make sure its name **Starts from capital letters **

// 3. In react you can only return a single component , so solve this problem , we enclose all the tags into a single empty component called **ReactFragment**. 

// 4. React create its own virtual dom , separate from the browser dom , then it matches the both dom and then makes changes , this is done so that we only change componenets which we want to 


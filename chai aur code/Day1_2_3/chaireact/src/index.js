import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App /> //this is a component 
  </React.StrictMode>
);


/*here we are rendering the app tag which is a custom componenet , this is possible due to jsx*/
// jsx --> it stands for javaScript XML , it is markup inserted into the js 
